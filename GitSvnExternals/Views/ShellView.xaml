<UserControl x:Class="GitSvnExternals.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Height="300" Width="500"
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid>
        <Grid.Resources>
            <Style x:Key="CommandButton" TargetType="Button">
                <Setter Property="Margin" Value="5,5,5,5" />
                <Setter Property="Width" Value="80" />
            </Style>
        </Grid.Resources>

        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center">Repository:</TextBlock>
                <TextBox Width="200" Margin="5,0,0,0" VerticalAlignment="Center">Paste reprepo path here...</TextBox>
                <Button Style="{StaticResource CommandButton}">Get Externals</Button>
                <Button Style="{StaticResource CommandButton}">Clone All</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>